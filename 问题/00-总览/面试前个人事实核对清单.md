# 面试前个人事实核对清单

> 这份文档只处理源码和设计稿无法证明的个人事实。三个项目统一按公司内部项目回答，忽略源码日期；但团队、投产、用户、模型、部署和指标不能由题库代编。方括号内容必须由本人根据内部记录填写。

## 1. 使用规则

1. 优先找内部设计文档、监控、流水线、工单、接入记录、周报和评测报告。
2. 一个数字至少准备：指标定义、基线、样本、时间窗、查询或报告来源。
3. 技术兼容能力不等于业务投产，例如“支持多个 Host”不能自动改写成“多个业务渠道已上线”。
4. 离线 Benchmark 不等于线上平均收益，特定测试配置必须连同基线一起说。
5. 暂时拿不到证据时，使用本页的安全回答，不猜团队人数、QPS、SLA 或模型名称。

## 2. 三个项目事实表

| 必填事实 | AI 周报系统 | CodeWiki | Agent Memory |
| --- | --- | --- | --- |
| 业务入口/用户 | [填写群组、团队或使用入口] | [填写页面、API、MCP 或内部入口] | [填写真实业务入口，不把 Host 适配当渠道] |
| 本人角色 | [需求/设计/编码/测试/发布各自责任] | [后端、图、检索、前端协作边界] | [Core、Adapter、Gateway、接入各自责任] |
| 团队 RACI | [谁提需求、开发、测试、部署、验收] | [同左] | [同左] |
| 部署拓扑 | [调度位置、数据库、模型平台、消息接口] | [本地/服务端、数据库、Worker、前端] | [OpenClaw/Hermes/Gateway/CLI 的真实部署] |
| CI/CD 与回滚 | [流水线、发布人、回滚方法] | [流水线、迁移、回滚方法] | [包/镜像、配置、回滚方法] |
| 使用规模 | [团队数、周报数、运行频率] | [用户、日查询、仓库规模] | [用户、请求、Session、记忆量] |
| 性能/SLA | [完成时间、失败补跑时限] | [索引耗时、查询 P95、并发] | [Recall P95、Capture/Seed 耗时、并发] |
| 当前状态 | [持续使用/维护/移交/技术验证] | [同左] | [同左] |
| 证据位置 | [报告、SQL、工单或日志] | [日志、Benchmark、接入记录] | [Benchmark、Trace、接入记录] |

## 3. 高频事实题与口语答案

### F1. 三个项目现在还在使用吗？

> 这题我会按项目分别回答，不用源码提交日期推断。AI 周报目前是[持续使用/维护/移交/停止]，证据是[内部周报或任务记录]；CodeWiki 是[状态]，最近的使用入口和维护人是[填写]；Agent Memory 是[状态]，真实接入范围是[填写]。如果某个项目只完成技术验证，我会直接说技术验证，不把它说成稳定投产。

### F2. 除了 AI 项目，你三年后端经历里还交付过什么？

> 我会选一个能查到代码、工单或发布记录的 Java 后端变更来讲：[业务背景]，[我负责的接口/批任务/数据库/并发问题]，[最难的技术点]，[怎么测试和发布]，[最终结果]。当前题库没有足够材料替我选择这个案例，所以面试前必须补一条真实经历；不能用三套 AI 项目反复回答所有后端问题。

### F3. 你说主导项目，团队到底有几个人？

> “主导”指技术主线由我负责，不等于团队只有我一个人。这个项目里需求由[填写]提出，方案由[填写]负责，后端核心由[填写]实现，前端由[填写]协作，测试、部署和验收分别由[填写]负责。我承担的是[填写]，没有承担的是[填写]。人数必须按真实 RACI 回答，没有记录时不说“2-3 人”这类模板数字。

### F4. 部署在哪里，CI/CD 和回滚是谁负责？

> 项目部署在[内部平台/主机/容器环境，填写真实名称]，制品通过[流水线]构建，测试门禁包括[填写]，发布由[填写角色]执行。配置和数据库变更通过[填写]管理，失败时回滚到[上一制品/旧配置/旧 Schema]。我负责[填写]，平台或运维负责[填写]。如果本人只参与联调、不操作生产发布，我会明确说出来。

### F5. 用户量、峰值 QPS 和 SLA 是多少？

> 我会先说明指标口径：用户是[账号/团队/调用方]，请求是[页面查询/API/自动任务]，统计窗口是[填写]。真实数据是 DAU[填写]、日请求[填写]、峰值 QPS[填写]、P95[填写]、SLA[填写]，来源是[Dashboard/日志查询]。当前材料拿不出这些数据时，我会说：“我能确认的是技术链路和容量风险，真实规模需要回到监控核验，我不现场编数字。”

### F6. 公司实际使用什么模型和调用渠道？

> 公司项目通过[内部智算平台/正规 API，填写]调用[模型与版本]，账号和密钥由[填写]管理；个人工具使用[合法账号或公司批准渠道]。路由或降级是否真实上线要按记录回答。没有实际模型名时，我只讲选型维度和升级流程，不根据当前流行模型倒推当时型号，也不描述账号共享或不透明中转。

### F7. Agent Memory 说覆盖多个业务渠道，具体是什么？

> 技术上已经有 OpenClaw 进程内 Hook、Hermes 类宿主的 Sidecar Gateway、Standalone CLI/Docker 等接入形态；这是多 Host 能力。真实业务渠道是[填写入口]，各自用户或请求是[填写]，由[填写]维护。拿不出接入记录时，我会把简历口径降为“具备多 Host 接入能力”，不说“覆盖多个业务渠道投产”。

### F8. CodeWiki 日均 50+ 查询是怎么统计的？

> 这里的查询定义是[Ask/API/MCP/页面操作，填写]，是否排除重试、健康检查和自动任务由[填写]规则决定；统计窗口是[填写]，去重口径是[填写]，来源是[日志或报表]。如果只有印象而没有日志，我不会继续报 50+，会改成“已有内部使用和反馈”，等拿到统计再恢复精确数字。

### F9. 61%、52% 和 48% 到 76% 怎么证明？

> 这三组数字来自特定 Benchmark，不是线上平均收益。回答时要固定数据集版本、模型、Prompt、上下文窗口、基线、随机性、重复次数和判定方式。当前材料能引用已有结果表，但实验元数据不完整时，我只说“在该配置下观察到”，不把最好一次结果推广到所有任务，也不补造 50 个 Session、200 道题和精确错误占比。

### F10. AI 周报为什么说一年节省约 50 人天？

> 原流程每周总人工投入约 8 到 10 小时，自动链路约 20 分钟。按一年约 50 个工作周、8 小时一个人天，毛节省约 48 到 60 人天，所以简历取接近下限的“约 50 人天”。这是工时估算，不是财务审计结果，还要扣人工复核、异常补数和维护成本；8 到 10 小时的原始记录位置是[填写]。

### F11. 你实际怎样使用 AI Coding？

> 我会用一个真实 Commit 或工单回答：[需求]，[我先写的规格和验收]，[AI 帮助检索/生成的部分]，[我改掉的错误或风险]，[测试暴露的问题]，[Review、发布和回滚]。AI 代码比例按真实统计回答；无论比例多少，需求判断、架构取舍、测试和最终责任都在我。

### F12. 薪资、住址、入职时间、加班和个人渠道怎么回答？

> 这些都是面试当日事实，题库不预填。我会直接、简短地回答当前薪资与期望区间[填写]、最快入职时间[填写]、可接受的工作节奏[填写]和真实信息渠道[填写]。对情感、家庭、征信等越界问题，使用主手册 Q217 的边界回答；对正式背调，只在用途、范围、保存和书面授权明确后配合。

## 4. 面试前关闭条件

- [ ] 三个项目的 RACI 均已填写，并与“独立负责/全栈”表述一致。
- [ ] 投产、业务渠道、日均查询、用户、QPS 和 SLA 都有来源，或简历已降级表述。
- [ ] 三组 Benchmark 数字能说明基线与实验边界，不引用虚构样本细节。
- [ ] 公司模型、个人工具和账号渠道均能合法、如实回答。
- [ ] 至少准备一条非 AI 的真实 Java 后端交付案例。
- [ ] 当前维护状态、CI/CD、发布和回滚责任已按项目确认。
